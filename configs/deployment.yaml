apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-kustomize-a
  template:
    metadata:
      labels:
        app: nginx-kustomize-a
    spec:
      containers:
      - image: nginx
        name: nginx